/**
 * external_input.cpp — Placeholder for external input bridge
 *
 * The actual C API implementation lives in prism/input.cpp where
 * it has access to the anonymous-namespace gPrismGeneralInputData.
 * This file exists only to satisfy the build system's `ls *.cpp` glob.
 */

// Intentionally empty — see prism/input.cpp for the implementation